# OpenClaw 部署仓库

此仓库包含 OpenClaw 服务器端部署所需的配置文件、脚本和工作区模板。

## 结构

- `服务器/` — OpenClaw Linux 服务器部署配置
  - `install.sh` — 一键安装脚本
  - `openclaw/` — Gateway 配置
  - `workspace/` — 工作区模板
  - `systemd/` — 系统服务
  - `cloudflare/` — Cloudflare Tunnel 方案
  - `nginx/` — Nginx 反向代理
  - `scripts/` — 管理脚本
