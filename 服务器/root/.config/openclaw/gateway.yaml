# OpenClaw Gateway 配置文件
# 上传到 /root/.config/openclaw/gateway.yaml

gateway:
  host: 127.0.0.1
  port: 8080

  # 【重要】请修改此密码！
  auth:
    token: "your-secret-token-here"

  stateDir: /data/.openclaw
  workspaceDir: /data/workspace

  remote:
    bind: loopback

logging:
  level: info
  format: text
